apiVersion: v1
kind: Service
metadata:
  name: "{{ include "prowlarr.fullname" . }}-metrics"
  labels:
    {{- include "prowlarr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 9707
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "prowlarr.selectorLabels" . | nindent 4 }}

machine:
  type: worker
  kubelet:
    extraArgs:
      rotate-server-certificates: true
  install:
    disk: ${install_disk}
    image: ${image}
  kernel:
    modules:
      - name: zfs
  sysctls:
    net.core.bpf_jit_harden: 1
  nodeLabels:
    feature.node.kubernetes.io/amd-gpu: true
